import 'package:flutter/material.dart';

class LabourWork extends StatelessWidget {
  const LabourWork({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [],
    );
  }
}
